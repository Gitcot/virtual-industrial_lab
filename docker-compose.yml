services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: vil_user
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: vil
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
